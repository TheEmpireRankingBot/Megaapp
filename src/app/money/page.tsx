import { ModulePlaceholder } from "@/components/module-placeholder";

export const metadata = { title: "Money" };

export default function MoneyPage() {
  return (
    <ModulePlaceholder
      title="Money"
      description="Expense logging, budgets, subscriptions, and monthly summaries."
      phase="Phase 2"
    />
  );
}
